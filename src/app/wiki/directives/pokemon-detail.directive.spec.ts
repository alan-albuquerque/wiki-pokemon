import { PokemonDetailDirective } from './pokemon-detail.directive';

describe('PokemonDetailDirective', () => {
  it('should create an instance', () => {
    const directive = new PokemonDetailDirective();
    expect(directive).toBeTruthy();
  });
});
